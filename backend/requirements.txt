# Every dependency is pinned: an unpinned LangChain/Qdrant/Ragas release can
# break CI or the container build with no change on our side.
# Versions below are the ones this project is verified against.

# ── Web Framework ─────────────────────────────────────
fastapi==0.111.1
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# ── LangChain Ecosystem ───────────────────────────────
# Generation goes through Cerebras' OpenAI-compatible endpoint (langchain-openai),
# so the google-genai and groq integrations are not installed.
langchain==0.3.30
langchain-core==0.3.86
langchain-community==0.3.27
langchain-huggingface==0.3.1
langchain-openai==0.3.35
langchain-qdrant==0.2.1
langchain-text-splitters==0.3.11

# ── Vector Database ───────────────────────────────────
qdrant-client==1.18.0

# ── Embeddings / Reranking ────────────────────────────
# Pulled in by langchain-huggingface; HuggingFaceCrossEncoder loads the
# ms-marco reranker through it, so it is declared explicitly.
# transformers is pinned too: a major bump there is the usual way a working
# sentence-transformers install breaks without any change on our side.
sentence-transformers==5.5.0
transformers==5.8.1
# torch is installed from the CPU wheel index in the Dockerfile (before this
# file) to keep the image small; the floor here just documents what we test on.
torch>=2.12.0

# ── Document Loading ──────────────────────────────────
pypdf==4.3.1
beautifulsoup4==4.12.3
lxml==5.3.0
requests==2.32.3

# ── BM25 (Hybrid Search) ─────────────────────────────
rank-bm25==0.2.2

# ── Tokenization ──────────────────────────────────────
tiktoken==0.12.0

# ── Observability ─────────────────────────────────────
langfuse==2.47.0

# ── Validation & Config ───────────────────────────────
pydantic==2.8.2
pydantic-settings==2.4.0
typing-extensions>=4.10.0
python-dotenv==1.0.1
pyyaml==6.0.2

# ── Retry / Backoff ───────────────────────────────────
tenacity==8.5.0

# ── HTTP ──────────────────────────────────────────────
httpx==0.27.0

# ── Evaluation ────────────────────────────────────────
ragas==0.3.2
datasets==2.21.0
pytest==8.3.2
pytest-asyncio==0.23.8
