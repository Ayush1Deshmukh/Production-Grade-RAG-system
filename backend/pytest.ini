[pytest]
# Only the real suite. The ad-hoc test_*.py probes at the backend root make live
# API calls at import time, so bare `pytest` must not pick them up.
testpaths = tests
addopts = -v --tb=short
